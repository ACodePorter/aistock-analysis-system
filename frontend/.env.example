# Frontend environment example
# If deploying separately from backend, set the backend public base URL (no trailing slash).
# During local dev with Vite (port 5174), proxy in vite.config.ts already forwards /api -> http://localhost:8090
# so you can leave this unset.
# Example for production:
# VITE_API_BASE=https://your-domain.com

